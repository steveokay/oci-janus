module github.com/steveokay/oci-janus/infra/scanner-plugins/dev-stub

// Single-file binary with no external deps. Pinned to the same toolchain
// as the rest of the monorepo so go.work doesn't fight us.
go 1.25.7
