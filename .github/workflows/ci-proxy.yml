name: CI — proxy

on:
  push:
    paths: ["services/proxy/**", "libs/**", "proto/proxy/**"]
  pull_request:
    paths: ["services/proxy/**", "libs/**", "proto/proxy/**"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - uses: golangci/golangci-lint-action@v6
        with:
          working-directory: services/proxy
          config: ../../.golangci.yml

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/proxy && go test -race ./...

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/proxy && make build
      - name: Build Docker image
        run: |
          docker build             --build-arg SERVICE=proxy             -f services/proxy/Dockerfile             -t ghcr.io/steveokay/oci-janus/proxy:${{ github.sha }}             .
      - name: Scan image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/steveokay/oci-janus/proxy:${{ github.sha }}
          exit-code: "1"
          severity: CRITICAL
