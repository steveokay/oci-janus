name: CI — storage

on:
  push:
    paths: ["services/storage/**", "libs/**", "proto/storage/**"]
  pull_request:
    paths: ["services/storage/**", "libs/**", "proto/storage/**"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - uses: golangci/golangci-lint-action@v6
        with:
          working-directory: services/storage
          config: ../../.golangci.yml

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/storage && go test -race ./...

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          cd services/storage && govulncheck ./...

  build:
    runs-on: ubuntu-latest
    needs: [lint, test, security]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/storage && make build
      - name: Build Docker image
        run: |
          docker build             --build-arg SERVICE=storage             -f services/storage/Dockerfile             -t ghcr.io/steveokay/oci-janus/storage:${{ github.sha }}             .
      - name: Scan image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/steveokay/oci-janus/storage:${{ github.sha }}
          exit-code: "1"
          severity: CRITICAL
