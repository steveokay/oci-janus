name: CI — gc

on:
  push:
    paths: ["services/gc/**", "libs/**", "proto/gc/**"]
  pull_request:
    paths: ["services/gc/**", "libs/**", "proto/gc/**"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - uses: golangci/golangci-lint-action@v6
        with:
          working-directory: services/gc
          config: ../../.golangci.yml

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/gc && go test -race ./...

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.23" }
      - run: cd services/gc && make build
      - name: Build Docker image
        run: |
          docker build             --build-arg SERVICE=gc             -f services/gc/Dockerfile             -t ghcr.io/steveokay/oci-janus/gc:${{ github.sha }}             .
      - name: Scan image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/steveokay/oci-janus/gc:${{ github.sha }}
          exit-code: "1"
          severity: CRITICAL
